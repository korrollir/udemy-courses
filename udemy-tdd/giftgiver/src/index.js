/* No test file for this component.  It is simply for hooking up to the DOM. */
import React from 'react'
import { render } from 'react-dom'

import App from './components/App'

import './index.css'

render(<App />, document.getElementById('root'))